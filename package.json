{
  "name": "loadbar-api",
  "version": "1.0.0",
  "private": true,
  "description": "Loadbar backend API — standalone Vercel serverless functions",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "mysql2": "^3.11.0"
  }
}
